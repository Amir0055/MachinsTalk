import { Path } from './Path';

export class Parameterss {

    id!: number;
    clee!: any;
    paramType!: ParamType;
    pathsss!: Path;
  }
 enum ParamType {
     QUERRY_PARAM ,
     PATH_VARIABLE,

   }
